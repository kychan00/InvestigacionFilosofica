name: Audit OpenAlex philosophy classifier v3.1

on:
  workflow_dispatch:

jobs:
  audit:
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install duckdb huggingface_hub

      - name: Run classifier v3.1
        run: |
          python scripts/openalex/sample_philosophy_classifier_v3_1.py

      - name: Upload classifier v3.1
        uses: actions/upload-artifact@v4
        with:
          name: openalex-philosophy-classifier-v3-1
          retention-days: 30
          compression-level: 0
          path: |
            artifacts/openalex/philosophy-classifier-v3-1.parquet
